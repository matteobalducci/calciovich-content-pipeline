# Calciovich Content Pipeline — runtime dependencies
# Python 3.11+ (uses zoneinfo from the standard library)
#
# Note: this repo is an extract and is not a runnable demo — it references a
# private configuration/data layer that lives elsewhere. These are the packages
# the scripts import, pinned to the majors they were developed against.

# --- Google / YouTube (Data API v3) ---
google-api-python-client>=2.100
google-genai>=0.3            # Gemini image generation (genera_immagini.py)
google-auth>=2.23
google-auth-oauthlib>=1.1
# cryptography is a transitive dependency (google-auth-oauthlib -> requests-oauthlib
# -> oauthlib[signedtoken]), not imported directly here — capped because newer
# releases stopped shipping macOS x86_64/universal2 wheels, which breaks `pip
# install` from source on an x86_64 interpreter (a real Intel Mac, or Rosetta —
# e.g. a stock Anaconda install on Apple Silicon) unless a local OpenSSL toolchain
# is set up for a from-source build. 44.x is the last release with those wheels.
cryptography<45

# --- Object storage (Cloudflare R2, S3-compatible) ---
boto3>=1.34

# --- Media generation & assembly ---
Pillow>=10.0
imageio-ffmpeg>=0.4.9
numpy>=1.26
edge-tts>=6.1            # free neural voiceover

# --- PDF / vector assets (certificates, plates) ---
reportlab>=4.0
svglib>=1.5

# --- Optional: paid voice provider, only used when explicitly enabled ---
elevenlabs>=1.0
